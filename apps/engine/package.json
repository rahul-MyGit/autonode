{
  "name": "engine",
  "description": "This will be the engine of the application. It will execute the workflows and flow tasks.",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/express": "^5.0.6"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@n8n/zod": "*",
    "bullmq": "^5.71.0",
    "express": "^5.2.1"
  }
}
